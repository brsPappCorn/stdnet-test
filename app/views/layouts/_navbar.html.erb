<div class="container">

  <nav class="navbar navbar-default navbar-custom">
    <div class="container-fluid">

      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <!--Logo-->
        <%= link_to image_tag('logo.png', alt: 'studNet | La red laboral de estudiantes universitarios.', class: 'navbar-logo'), root_path, class: '' %>
        <!--./Logo-->
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="">

        <ul class="nav navbar-nav navbar-right">
          <li class="<%= is_active?(root_path) %>">
            <%= link_to 'Home', class: '' %>
          </li>
          <li class="<%= is_active?('#') %>">
            <%= link_to 'Estudiantes', class: '' %>
          </li>
          <li class="<%= is_active?('#') %>">
            <%= link_to 'Empresas/Personas', class: '' %>
          </li>
          <li class="<%= is_active?('http://www.studnet.com.co/acerca-de-studnet/') %>">
            <%= link_to 'Acerca de', 'http://www.studnet.com.co/acerca-de-studnet/',class: '' %>
          </li>
          <li class="<%= is_active?('#') %>">
            <%= link_to 'Contacto', class: '' %>
          </li>

          <!-- Account Menu Dropdown. Depends on the type of user signed in. -->
          <% if user_signed_in? %> <!-- TODO: Update dropdown actions depending on the role -->
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  Perfil
                  <span class="caret"></span>
                </a>
                <ul class="dropdown-menu dropdown-menu-fixed">
                  <li>
                    <%= link_to 'Action', '#'  %>
                  </li>
                  <li>
                    <%= link_to 'Action', '#'  %>
                  </li>
                  <li role="separator" class="divider"></li>
                  <li>
                    <%= link_to destroy_user_session_path, method: :delete do %>
                        Sign out
                    <% end %>
                  </li>
                </ul>
              </li>
          <% end %>
          <!-- Account Menu Dropdown. -->

        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>

</div>