<p id="notice"><%= notice %></p>
<section class="studNet-wrapper">
  <div class="container">

    <div class="row">
      <div class="col-md-12">

        <h1>Lista de Notification</h1>

        <table class="table">
          <thead>
          <tr>
             <th>Name</th>
             <th>Body</th>
             <th>User</th>
             <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
           <% @notification_posts.each do |notification_post| %>
            <tr>
              <td><%= notification_post.name %></td>
              <td><%= notification_post.body %></td>
              <td><%= notification_post.user.first_name %></td>
              <td><%= link_to 'Show', notification_post, class: 'btn btn-default btn-studnet-details' %></td>
              <td><%= link_to 'Edit', edit_notification_post_path(notification_post), class: 'btn btn-default btn-studnet-details' %></td>
              <td><%= link_to 'Destroy', notification_post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default btn-studnet-details' %></td>
            </tr>
          <% end %>
          </tbody>
        </table>

        <%= paginate @notification_posts %>

      </div>
    </div>
    <%= link_to 'New Notification Post', new_notification_post_path %>
  </div>
</section>
