<%= form_for(@administrator) do |f| %>
    <%= render 'shared/error_messages', object: @administrator %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
